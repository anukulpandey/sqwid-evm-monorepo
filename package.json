{
  "name": "sqwid-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @sqwid/contracts compile && yarn workspace @sqwid/backend build && yarn workspace @sqwid/client build",
    "test": "yarn workspace @sqwid/contracts test && yarn workspace @sqwid/backend test && yarn workspace @sqwid/client test --watchAll=false",
    "dev": "docker compose up --build",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down -v",
    "db:migrate": "yarn workspace @sqwid/backend db:migrate"
  },
  "resolutions": {
    "@babel/core": "^7.16.0"
  }
}
